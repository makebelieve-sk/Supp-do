// Инициализация состояния для раздела "Помощь"
const initialState = {
    help: [],               // Записи раздела
    rowDataHelp: null,      // Редактируемая запись
    errorRecordHelp: null,      // Ошибка в записи
    errorTableHelp: null,       // Ошибка в таблице
};

export default initialState;