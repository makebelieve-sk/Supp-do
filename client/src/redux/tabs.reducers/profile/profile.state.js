// Инициализация состояния для раздела "Профиль"
const initialState = {
    profile: null,      // Редактируемая запись
    errorRecordProfile: null,      // Ошибка в записи
};

export default initialState;