// Инициализация состояния для раздела "Пользователи"
const initialState = {
    users: [],              // Записи журнала
    rowDataUser: null,      // Редактируемая запись
    errorRecord: null,      // Ошибка в записи
    errorTable: null,       // Ошибка в таблице
};

export default initialState;